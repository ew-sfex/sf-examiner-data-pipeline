name: SF Examiner Data Pipeline

on:
  schedule:
    # Run daily at 12:00 PM UTC (4:00 AM PST / 4:00 AM PDT)
    - cron: '0 12 * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  update-charts:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyarrow
        
    - name: Run RDC Metro Data Download
      env:
        DATAWRAPPER_API_KEY: ${{ secrets.DATAWRAPPER_API_KEY }}
        DATASF_APP_TOKEN: ${{ secrets.DATASF_APP_TOKEN }}
      run: python rdc_inventory_pipeline.py
      
    - name: Run RDC County Data Download
      env:
        DATAWRAPPER_API_KEY: ${{ secrets.DATAWRAPPER_API_KEY }}
        DATASF_APP_TOKEN: ${{ secrets.DATASF_APP_TOKEN }}
      run: python rdc_inventory_county_pipeline.py
      
    - name: Run RDC Metro Charts Update
      env:
        DATAWRAPPER_API_KEY: ${{ secrets.DATAWRAPPER_API_KEY }}
        DATASF_APP_TOKEN: ${{ secrets.DATASF_APP_TOKEN }}
      run: python sf_rdc_inventory_charts.py
      
    - name: Run RDC County Charts Update
      env:
        DATAWRAPPER_API_KEY: ${{ secrets.DATAWRAPPER_API_KEY }}
        DATASF_APP_TOKEN: ${{ secrets.DATASF_APP_TOKEN }}
      run: python sf_rdc_county_charts.py
        
    - name: Run 911 Maps Update
      env:
        DATAWRAPPER_API_KEY: ${{ secrets.DATAWRAPPER_API_KEY }}
        DATASF_APP_TOKEN: ${{ secrets.DATASF_APP_TOKEN }}
      run: python sf_911_maps.py
      
    - name: Run 911 Charts Update
      env:
        DATAWRAPPER_API_KEY: ${{ secrets.DATAWRAPPER_API_KEY }}
        DATASF_APP_TOKEN: ${{ secrets.DATASF_APP_TOKEN }}
      run: python sf_911_pipeline.py
      
    - name: Run 311 Maps Update
      env:
        DATAWRAPPER_API_KEY: ${{ secrets.DATAWRAPPER_API_KEY }}
        DATASF_APP_TOKEN: ${{ secrets.DATASF_APP_TOKEN }}
      run: python sf_311_maps.py
      
    - name: Run 311 Charts Update
      env:
        DATAWRAPPER_API_KEY: ${{ secrets.DATAWRAPPER_API_KEY }}
        DATASF_APP_TOKEN: ${{ secrets.DATASF_APP_TOKEN }}
      run: python sf_311_pipeline.py
      
    - name: Run Building Permits Maps Update
      env:
        DATAWRAPPER_API_KEY: ${{ secrets.DATAWRAPPER_API_KEY }}
        DATASF_APP_TOKEN: ${{ secrets.DATASF_APP_TOKEN }}
      run: python sf_building_permits_maps.py
      
    - name: Run Building Permits Charts Update
      env:
        DATAWRAPPER_API_KEY: ${{ secrets.DATAWRAPPER_API_KEY }}
        DATASF_APP_TOKEN: ${{ secrets.DATASF_APP_TOKEN }}
      run: python sf_building_permits_pipeline.py
      
    - name: Run Business Openings Maps Update
      env:
        DATAWRAPPER_API_KEY: ${{ secrets.DATAWRAPPER_API_KEY }}
        DATASF_APP_TOKEN: ${{ secrets.DATASF_APP_TOKEN }}
      run: python sf_business_openings_maps.py
      
    - name: Run Business Openings Charts Update
      env:
        DATAWRAPPER_API_KEY: ${{ secrets.DATAWRAPPER_API_KEY }}
        DATASF_APP_TOKEN: ${{ secrets.DATASF_APP_TOKEN }}
      run: python sf_business_openings_pipeline.py
      
    - name: Download RDC Metro Inventory Data
      run: python rdc_inventory_pipeline.py
      
    - name: Download RDC County Inventory Data
      run: python rdc_inventory_county_pipeline.py
      
    - name: Update RDC Metro Charts
      env:
        DATAWRAPPER_API_KEY: ${{ secrets.DATAWRAPPER_API_KEY }}
      run: python sf_rdc_inventory_charts.py
      
    - name: Update RDC County Charts
      env:
        DATAWRAPPER_API_KEY: ${{ secrets.DATAWRAPPER_API_KEY }}
      run: python sf_rdc_county_charts.py
      
    - name: Upload logs as artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: pipeline-logs
        path: |
          *.log
        retention-days: 30 